# Отчёт: версия 3.0 (14 сентября 2026)

## 1. Фиксация stable 2.0.0

- Git: `58425b6c9b2f4ee210de8196d10281693ab1dbb3`, аннотированный тег `v2.0.0-demo-stable`, дерево `fc0c27091c15eebec52e7ba43dfc2a044a3dca11`, рабочая копия чистая.
- Полная копия: `backups\turism_hab_v2.0.0_2026-09-14` — 122 496 файлов, 17,1 ГБ.
- Манифест `…sha256.csv` (`relative_path,size,sha256`), агрегированный отпечаток
  `2ae588ac884850ee80bea686604c3ac8f7e153933dcf94103b8d3bccc1e63139`.
- Source-архив `git archive`: `aae129c574bb0a9c044417da8f0bb17c9eb37719fda9cd5c2fa1822dcb5635a0`.
- `…critical.json`: каталог, `dataset/manifest.csv`, агрегат `dataset/processed` (612 файлов,
  `529e9e79…`), FAISS и metadata, CLIP ViT-B-16 (`5806e77c…`), `runtime/users.db` (`PRAGMA integrity_check = ok`,
  открыта read-only/immutable), `uv.lock`, npm lock, `.venv`, `node_modules`, Docker-образ
  `discover-khabarovsk-krai:local` = `sha256:4f89101a7d535997973f8d7acfa1254b4dcecb98c48cceb1e8f7ca636e7d3397`.
- `verify-backup.ps1` (read-only, параллельный SHA-256): первая полная сверка — 0 пропущенных, 0 изменённых,
  0 лишних (490 с). Самотест `scripts\test-verify-backup.ps1`: обнаружены изменение того же размера,
  удаление, добавление; подделка манифеста — код 2.
- `smoke-test-backup.ps1` (прокси на мёртвый порт, `HF_HUB_OFFLINE=1`, без байткода): API `ready`,
  индекс готов (39 опубликованных классов), статический веб 200, 44 объекта, распознавание эталонного
  фото «Амурский утёс» (score 0,936), Streamlit `ok`, Expo Web (index и bundle 200), CLIP локальный — PASSED.
- Повторная сверка backup после всей работы в v3 и smoke-теста: 122 496 из 122 496, 0 пропущенных,
  0 изменённых, 0 лишних, манифест и source-архив совпадают (`verify-report-2026-09-14-after-v3.json`).
- Stable-папка: git чистый, хеши каталога, manifest, FAISS, `uv.lock`, npm lock совпадают.
  Stable-контейнеры Docker (`:8000`, `:8501`) работали одновременно с v3 — healthy.
- Запуск stable одной командой: `..\start-stable-v2.ps1` (или `-Docker`).

Ограничение: `.venv` backup ссылается на uv-интерпретатор в `turism_hab\.runtime\python`; backup
запускается, пока существует исходная папка, или после восстановления по исходному пути.

## 2. Отдельный клон

- `turism_hab_v3`, ветка `feature/v3-tour-guide`, версия `3.0.0-dev.1`; порты 8100/8601/8082.
- Изоляция: `.venv` клона переведён на собственный интерпретатор (раньше указывал на stable),
  ASCII-алиас `E:\turism_hab_v3_runtime`, AVD-конфиги эмулятора перенаправлены на него, отдельные ключи
  AsyncStorage (`v3:*`), slug/package приложения `…-v3`, FAISS temp в runtime клона.
- `dataset/manifest.csv` и `uv.lock` скопированы байт-в-байт из stable: git-checkout поменял переводы строк,
  из-за чего FAISS отклонял бы индекс. В `git status` они видны как изменённые только по EOL.
- `scripts\verify_assets.py`: каталог, manifest, FAISS, metadata, CLIP и 612 изображений совпадают со stable.
  FAISS не перестраивался.

## 3. Интерфейс

- Expo SDK 54 сохранён. Единая тема (`theme.js`), `MaterialCommunityIcons`, компактные заголовки,
  новые экраны `PlaceDetail`, `Route`, `Guide` в Stack поверх существующих вкладок, кнопка «Гид» в шапке,
  карточки с фото, категорией, районом, сезоном, сложностью и временем, touch-target ≥ 44 px, safe area.
  Expo Doctor 18/18 (исправлен несовместимый `expo-font` 57 → 14.0.x), web-export успешен.
- Проверено в Pixel 7 Emulator: распознавание, каталог, карточка места, маршрут, гид с ответом по маршруту.
- Статический веб без сборщика: фильтры (категория, район, сезон, формат), модальная панель карточки,
  панель маршрута с таймлайном, панель ИИ-гида, deep links (`#catalog`, `#place-29`, `#guide`), экранирование
  всех данных. Проверено на 1440 и 768 px и в iframe 390 px.
- Streamlit: общая палитра, статусы API, индекса распознавания, базы знаний и ИИ-гида.

## 4–6. Контент, маршруты, ИИ-гид

- 44 валидируемые карточки: 22 `source_checked` (сверены с habtravel.ru), 22 `needs_editor_review`.
- FTS5-база `kb2-b0e9f10397a6` с SHA-256 и автопересборкой.
- Маршруты: старый формат совместим, новые поля и сводка, диапазоны для удалённых объектов.
- Ollama в `.runtime\ollama` (CPU), модель `qwen2.5:1.5b-instruct`. Ответы на CPU — 3–11 с
  (загрузка модели ~10 с при первом запросе).
- Реальная модель 1.5B заметно ошибается, поэтому добавлены проверки: числа, цены, часы, телефоны,
  лексическое покрытие пунктов списка, повторы, соответствие итогу маршрута; вопросы безопасности,
  доступности, цен и расписаний отвечаются дословно из базы.
- Docker-образ `discover-khabarovsk-krai:v3`: контейнер healthy за ~20 с, `status=ready`, база знаний 44,
  доступ к Ollama хоста через `host.docker.internal`, ответ `local_llm`.

## 7. Проверки

- `pytest`: 65 passed; `ruff`: без замечаний.
- Покрыты: 44 карточки и отрицательные случаи валидации; старый/новый формат маршрутов, формулы скоростей,
  удалённые объекты, `available_minutes`, муниципалитет; история, экипировка, сезон, длительность;
  неизвестный объект, неоднозначный вопрос, prompt injection (в сообщении и истории), выключенный Ollama,
  тайм-аут, выдуманные цены/числа/пункты, лимиты сообщения и истории, 20 запросов в минуту;
  регрессия API, авторизации, истории распознаваний, запрета reindex для пользователя.

## Открытые вопросы

- 22 карточки требуют редакторской проверки фактов перед публикацией.
- Качество формулировок модели 1.5B ограничено; при сомнении гид возвращает проверенный текст из базы.
- `npm audit` для Expo SDK 54 по-прежнему сообщает транзитивные уязвимости (исправление требует миграции SDK).
- Изменения v3 не закоммичены — ветка `feature/v3-tour-guide` готова к коммиту.
